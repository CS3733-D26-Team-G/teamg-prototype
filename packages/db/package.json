{
  "name": "@repo/db",
  "private": true,
  "type": "module",
  "scripts": {
    "db:generate": "prisma generate",
    "db:generate-prod": "cross-env DOTENV_FILE=.env.production prisma generate",
    "db:migrate-dev-prod": "cross-env DOTENV_FILE=.env.production prisma migrate dev",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^7.6.0",
    "dotenv": "^17.3.1",
    "prisma-zod-generator": "^2.1.4"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "cross-env": "^10.1.0",
    "prisma": "^7.6.0"
  }
}
